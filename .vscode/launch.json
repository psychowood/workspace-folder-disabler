{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Workspace Folders Disabler",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder:FolderCloser}"
      ],
      "outFiles": [
        "${workspaceFolder:FolderCloser}/out/**/*.js"
      ],
      "preLaunchTask": "npm: compile"
    }
  ]
}
